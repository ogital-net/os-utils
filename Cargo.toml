[package]
name = "os-utils"
version = "0.1.0"
edition = "2021"
rust-version = "1.74"

[dependencies]
libc = "0.2"
rand_core = { version = ">= 0.10", optional = true }

[features]
rand = ["dep:rand_core"]
default = []

[build-dependencies]
cc = "1"